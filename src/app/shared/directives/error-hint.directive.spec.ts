import { ErrorHintDirective } from './error-hint.directive';

describe('ErrorHintDirective', () => {
	it('should create an instance', () => {
		const directive = new ErrorHintDirective();
		expect(directive).toBeTruthy();
	});
});
