import { ChipIconDirective } from './chip-icon.directive';

describe('ChipIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipIconDirective();
    expect(directive).toBeTruthy();
  });
});
