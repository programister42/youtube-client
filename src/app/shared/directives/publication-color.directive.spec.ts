import { PublicationColorDirective } from './publication-color.directive';

describe('PublicationColorDirective', () => {
  it('should create an instance', () => {
    const directive = new PublicationColorDirective();
    expect(directive).toBeTruthy();
  });
});
