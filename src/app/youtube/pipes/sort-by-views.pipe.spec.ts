import { SortByViewsPipe } from './sort-by-views.pipe';

describe('SortByViewsPipe', () => {
	it('create an instance', () => {
		const pipe = new SortByViewsPipe();
		expect(pipe).toBeTruthy();
	});
});
